#-*-coding:utf8-*-
from etool import logs,queue
import json

__processor__ = 'finance_model_test'
log = logs.getLogger(__processor__)
def test():
    port = 30115
    with queue.open(port,'w',capture=True) as outq:
        log.debug('Writing to %d' %(port))
        outq.write(json.dumps(msgObj, encoding='utf8'))
